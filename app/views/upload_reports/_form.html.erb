<%= form_for(upload_report) do |f| %>
  <% if upload_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upload_report.errors.count, "error") %> prohibited this upload_report from being saved:</h2>

      <ul>
      <% upload_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
